{
  "name": "tip-travel-intelligence-and-planner",
  "private": true,
  "scripts": {
    "prepare": "node -e \"if(process.env.CI)process.exit(0)\" || husky install",
    "lint-staged": "lint-staged --cwd frontend --relative"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7"
  }
}
